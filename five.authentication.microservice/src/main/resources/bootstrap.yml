spring:
  application:
    name: five-authentication-microservice
  cloud:
    zookeeper:
      connect-string: jozka.ojejek.com:2181,wera.ojejek.com:2181,zuza.ojejek.com:2181
      discovery:
        enabled: false
        instanceId: ${spring.application.name}
      config:
        enabled: true
        root: config
        defaultContext: application
